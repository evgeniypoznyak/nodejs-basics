<h1>{{ title }}</h1>
<p>Welcome to {{ title }}</p>
{{# if condition }}
    Condition is met
{{ else }}
    Condition is no met
{{/if}}

{{# each anyArray as |val key|}}
    <br>{{ key }} - {{ val }}
{{/each}}